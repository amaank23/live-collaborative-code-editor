{
  "name": "live-collaborative-code-editor",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/client",
    "apps/server"
  ],
  "scripts": {
    "dev": "concurrently --names client,server --prefix-colors cyan,yellow \"npm run dev --workspace=apps/client\" \"npm run dev --workspace=apps/server\"",
    "build": "npm run build --workspace=apps/client && npm run build --workspace=apps/server",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
